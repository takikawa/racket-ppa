#!/bin/sh -e

# Source debconf library.
. /usr/share/debconf/confmodule

if [ ! -w /usr/local ]; then
    db_input medium mzscheme/create_local_plt_dir_failed || true
fi
db_input medium mzscheme/create_zo_files_now || true
db_go

db_input high mzscheme/run_webserver || true
db_go
