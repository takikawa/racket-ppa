#!/bin/sh
set -e

case "$1" in purge|remove)

	if [ -d /usr/local/lib/plt/collects ]; then
		rmdir --ignore-fail-on-non-empty \
			/usr/local/lib/plt/collects /usr/local/lib/plt
	fi

esac

#DEBHELPER#
